const Resources = () => {
  // I've disabled this  since everything gets pulled from the same table -Z
  // dynamic-events.tsx is pulling all the resources now
  return null;
};

export default Resources;
